# Pinned dependencies (CLAUDE.md §3: pin all dependency versions).
# Extend as the pipeline grows; update SETUP.md when you do.
numpy==2.4.6
shapely==2.1.2
pytest==9.1.1
opencv-python==5.0.0.93
scikit-image==0.26.0
pycocotools==2.0.11
