# Environments & secrets
.venv/
venv/
.env
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Logs
logs/*
!logs/.gitkeep

# Data outputs (large / regenerable — never commit)
data/

# Model checkpoints (hundreds of MB; live on the GPU box / local disk only)
checkpoints/

# Third-party clones (commit hash recorded in envs/ + SETUP.md instead)
third_party/
